CREATE TABLE Servicios (
    id int NOT NULL IDENTITY(1, 1),
    nombre varchar(60),
	codigo int,
	cliente varchar(30),
	aeronave varchar(30),
	division varchar(25),
	departamento varchar(8),
    CONSTRAINT PK_id PRIMARY KEY (id)
);

insert into [dbo].[Servicios] (nombre, codigo, cliente, aeronave, division, departamento)
values ('', 0, '', '', '', '')